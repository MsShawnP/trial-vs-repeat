[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lailara-palette"
version = "2.1.0"
description = "Color constants from the Lailara Design System v2"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    {name = "Shawn Phillips", email = "msshawnp@gmail.com"},
]

[project.urls]
Repository = "https://github.com/MsShawnP/lailara-design-system"
